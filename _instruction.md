# Corrections  

There are some things I want to fix. I want you to add these changes to the 
CLAUDE.md file so that you don't do them again also.  

-   On the global.css file, add the `theme-v*` as it's own class, not at the 
    same level as root. Don't do this: `:root.theme-v2.dark`, do this: 
    `.theme-v2.dark`.  
-